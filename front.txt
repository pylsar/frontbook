Jira:
	# - нумерация пунктов
	(/) - галочка напротив пункта
	!name of picture.png|thumbnail!
	заголовок с подсветкой:	
						{panel:title=Мой заголовок|borderStyle=dashed|borderColor=#ccc|titleBGColor=#F7D6C1|bgColor=#FFFFCE}
						блок текста, окруженный *панелью*
						еще _другая_ линия
						{panel}

css:
	чтобы убрать при клике placeholder:
								input:focus::-webkit-input-placeholder { color:transparent; }
								input:focus:-moz-placeholder { color:transparent; } /* FF 4-18 */
								input:focus::-moz-placeholder { color:transparent; } /* FF 19+ */
								input:focus:-ms-input-placeholder { color:transparent; } /* IE 10+ */
	> - Непосредственное подчинение.в div есть span и p,то div > p{color:red;} то красный цвет будет только у p, div{color:red;} красный цвет у всех будет	 
	overflow: auto - добавить полосу прокрутки при необходимости.
	text-indent: 10px - Устанавливает величину отступа первой строки блока текста (например, для абзаца <p>). Воздействия на все остальные строки не оказывается. Красная строка
	Важно следить за css гигиеной.
	Не рекомендуется использовать вложения. ul li a  и тд, так как сначала css найдёт все a потом все li и тд. это тормозит работу приложения.
	resize: none: Размеры элемента не изменяются, убирает у textarea штрихи снизу-справа	
	border-image — весьма забавное свойство, которое позволяет обрамлять контейнер неким изображением. 
	align-items работает когда есть display: flex; и height;
	z-index работает когда есть position:...
	~ - выделяет все соседние объекты, которые идут после пример:
												h2 ~ p {} - отработает все параграфы, которые идут после h2
	+ - выделяет только следующий элемент. пример:
												h2 + p {} - отработает только следующий параграф
	разделить текст на 3 колонки(3 <p> внутри дива): 
												column-count: 3;											
	mix-blend-mode: определяет режим смешивания цветов выбранного элемента с низлежащими слоями. Плохая кроссбраузерность. Пример:
																									mix-blend-mode: screen;
	text-overflow: ellipsis; - ставит многоточие и урезает шрифт когдане помещается текст
	пример градиента:
					background-image: linear-gradient(#999 15%,white), linear-gradient(#999 15%,white);
	переменные css: :root {  --main-color: #ff6f69;} - задаём цвет переменной и выводим ее в нужном месте: .title { color: var(--main-color);}
					можно добавлять второй цвет(фолбэк) на случай если первая переменная не определена: .title {color: var(--main-color, black); }
	E + F - стиль для элементов F, следующими непосредственно за элементами E на одном уровне вложенности
	E > F - стиль для элементов F, вложенных в элементы E, и находящиеся на первом уровне вложенности
	E F - стиль для элементов F, вложенных в элементы E, и находящиеся на любом уровне вложенности
	E ~ F - стиль для элементов F, следующими за элементами E на одном уровне вложенности	
	em - html { font-size: 100% } /* это значит, что по умолчанию размер шрифта будет 16px*/ при этом em=16px	
	сначала hover а потом after пример: closeButton:hover::after {}		
	ссылка на главную: <a href="/"></a>
	text-decoration: line-through; перечеркивает надпись

html: 
	<figure></figure> - Используется для группирования любых элементов, например, изображений и подписей к ним.
	<article></article> - Задает содержание сайта вроде новости, статьи, записи блога, форума или др.
	Выпадающий список: пример:
								<select >
								  <option>макарошки</option>
								  <option>Греча</option>
								  <option>пельмени</option>
								</select>
	<sub></sub> -Отображает шрифт в виде нижнего индекса. Текст при этом располагается ниже базовой линии остальных символов строки и уменьшенного размера.(для формул и тд)
	<sup></sup>	- Отображает шрифт в виде верхнего индекса. Шрифт при этом отображается выше базовой линии текста и уменьшенного размера.(для степени)		
	<details>
		<summary>Информация об авторе</summary>
  		<p>Бендер Родригез</p>
  	</details>
  	srcset - Список из одного или нескольких значений, разделённых запятыми, указывающих набор возможных изображений для отображения в браузере.
  		пример: <img src="1.png" srcset="1x2.png 2x"> - первая картинка для виндовс, вторая для маков. Браузер выбирает наиболее подходящее изображение из списка, к примеру, учитывая плотность пикселей устройства, и отображает его.
  	всем ссылкам, которые ведут не на наш сайт даём	 rel="nofollow"
  	<h3> не должна быть выше <h2>
  	<wbr> - тег переноса строки
	 атрибут contenteditable - Сообщает, что элемент доступен для редактирования пользователем — можно удалять текст и вводить новый. Также работают стандартные команды вроде отмены, вставки текста из буфера и др.
		пример: <h1 contenteditable="true">Тортуга</h1>	
	якорь на ссылку. пример: <a href="qq.ru#название якоря"></a>, на сайте qq.ru в нужном месте ставим name=название якоря



pyCharm: 	
		cntrl shift F - поиск
		Ctrl+Tab - переключение между открытыми окнами
		shift+shift - поиск по всем разделам
		Ctrl+/ - комментирование выделенной области
		Ctrl+ Shift + / - многострочное комментирование выделенной области
		Ctrl+N - поиск классов
		F11	Поставить или снять закладку	
		Ctrl + F11	Аналогично с присвоением буквы или цифр
		Shift + F11	Переход к закладке (удаление — клавишей Delete)
		Alt + left/right - переключение между окнами
JS : 
	let -нельзя объявлять дважды одну и туже переменную.
	let x; x=10; - правильная запись        x=10; let x; - не правильная запись
	let объявленная глобально, видна всегда, в то время как let, объявленная внутри {} видна только внутри {}
	const - нельзя изменять
	undefind - переменная есть, но ей ничего не присвоено
	is not defind - переменная не объявлена
	hoisting - поднятие переменной. тоесть если переменная объявлена после её использования, то JS перенесёт ее наверх(во время инициализации а не в коде). работает только с var;
	дроби пишутся через точку
	typeof - показывает тип данных (строка число NAN булиан...)
	% - остаток от деления пример: 5%2=1
	parseInt() - преобразует в целое число
	parseFloat()- преобразует в дробь
	x+=y тоже что и	x=x+y
	метод push добавляет значение в конец массива. пример: array.push('hello'); 
	метод pop удаляет пследний элемент массива. пример: array.pop();
	метод shift удаляет первый элемент массива. пример: array.shift();
	метод unshift добавляет значение в начало массива. пример: array.unshift('hello'); 
	split разбивает строку на массив
	trim - избавляется от пробелов
	join - берет массив и объединяет в строку с помощью указаного разделителя. array.join('-')
	||(или) - запинается на правде
	&&(и) - запинается на лжи
	toFixed - округлить число. пример toFixed(1) - округлим до 1 знака после запятой
	класс - это шаблон для объектов. чтобы штамповать однотипные объекты. пример:
																				class Cat {
																				constructor(length, age){
																				this.length = length;
																				this.age = age;
																					}
																				}
																				var cat1 = new Cat(0.5, 5);
																				var cat2 = new Cat(1, 7);
	прототип - как бы наследование. ссылка на другой объект где указаны эти свойства.
	parseInt и parseFloat преобразуют строку символ за символом пока это возможно. пример: 12px ; alert(parseInt('12px')) будет равна 12 так как остановится на 'p' 


phpStorm:
		постановка курсора на несколько строк делается через alt	
		Edit - Find - Repalce in path - массовый поиск и замена

медиазапросы для кроссбраузерности:

медиазапросы:			
			@media all and (-ms-high-contrast: none), (-ms-high-contrast: active) {		/*для IE*/
				.technology-mem-indicators {
					margin-top: 44px;
				}
			}	

			@-moz-document url-prefix() {		/*для firefox*/
				.technology-mem-indicators {
					margin-top: 44px;
				}		

			@supports (-ms-ime-align:auto) {	/*для edge*/
				.technology-mem-indicators {
					margin-top: 44px;
				}
			}	

префиксы:
			* {
			-webkit-box-sizing: border-box;
			-moz-box-sizing: border-box;
			-ms-box-sizing: border-box;
			}
			-webkit-: браузеры Chrome, Safari, Opera;
			-moz-: браузер Mozilla Firefox;
			-ms-: браузер Internet Explorer

саппорты: 	@supports (display: grid) {
			    .main {
			        display: grid;
			    }
			}

адаптив:   	@media screen and (max-width: 1290px) {
			     .product-card-wrap {
					 &:hover {
						&:nth-child(5n+5) {
						width: 258.5px!important;
						margin: -13.5px 0px -13.5px -6.5px;
						}
					 }
				 }
			}			


backbone js :
			Зависит от underscore.js  и без него работать не будет. сначала подключаем underscore.js  а затем backbone.js


sublime :	
			Ctrl+Shift+ вверх - строку вверх
			Ctrl+Shift+вниз - Опустить строку вниз
			Продублировать строку - Ctrl+Shift+D
			
JQuery  :
			$(document).ready (function(){ тут пишется весь код}); - означает, что jquery начнёт свою работу после полной загрузки сайта
			$("h1").css("color","green"); - пример семантики JQuery
			пример:
				$(document).ready(function(){
				 	 $('#btn').on('click', function(){
						    $('#btn').addClass('bordered');
						    $('h1').addClass('selected');
						    $('p').css('fontSize','50px');
					  });
				});
			$('a[href^="http"]') - все ссылки которые начинаются на http	
			$('a[href$=".ru"]') - все ссылки которые заканчиваются на .ru
			$('p:eq(0)') - выберет самый первый абзац
			$('h3:has(span)') - выберет все h3 у которых внутри есть span
			Каждая клавиша имеет свой код. например Esc имеет код 27 и мы можем отловить нажатие каждой клавиши
			Пример: когда мы на ESC хотим повесить какоето действие
					$('#inputid').on('keyup', function(e){
						if (e.which == 27) {
						alert('it was escape key')
						}
					});
			$('id').hide(1000).delay(2000).show(1000) -скрываем за секунду, ждём 2 секунды, показываем за секунду

Adobe XD :
			пробел +   левая мышка - перемещает по горизонтали


SVG: 
		fill - цветовая заливка
		stroke - обводка контура
LESS:
		Комментарии:
			 либо // - для одной строки, либо /**/ - для нескольких строк
			/*//*/ - такую конструкцию использовать можно. /*/**/*/ - такую конструкцию использовать нельзя
			/*!!*/ - особый вид комментария, который не удалится при компеляции CSS
		Переменные:
			@dark: #333; 
			.selector { background-color: @dark;}
SourceTree: 
			Создал ветку в Jira(всегда от мастера)
			захожу в SourceTree и нажимаю fetch
			захожу в свою ветку
			в File Status плюсиками добавляю измения и делаю коммит(название задачи + комент)
			Нажимаю Push
			захожу в dev двойным кликом
			если есть pullы то нажимаю pull
			правой кнопкой мыши по задаче и нажимаю merge
			push
			возвращаюсь в свою ветку
SEO:	
	Чтобы запретить поисковым роботам Яндекс, Google и другиv индексировать сайт создайте файл robots.txt в корневой папке вашего сайта. 
	и прписываем:
				User-agent: *
				Disallow: /
				Host: www.sitename.ru
	h1 не даём классов и айди(оборачиваем в див и диву даём класс)
	структуру не нарушать h3 не может быть выше h1	
	Поисковики нормально ранжируют страницы сайтов на три уровня вложенности, страницы, которые не найти в три клика с главной, останутся не изученными и не займут высоких позиций в выдаче.



console: document.body.contentEditable=true - позволяет писать тэги и текст прямо в браузере.	
		пример: выбираем текст и правой кнопкой мыши edit html и можно добавлять теги и текст.		 


cntrl+F5  - перезагружает страницу игнoрируя кэширование
Shift + F5 / Ctrl + Shift + R - Обновить текущую страницу без учета кешированного контента





